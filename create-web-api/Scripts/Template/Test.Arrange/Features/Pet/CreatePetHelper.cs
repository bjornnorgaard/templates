using System;
using Application.Features.[Entity];

namespace Test.Arrange.Features.[Entity]
{
    public static class Create[Entity]Helper
    {
        public static Create[Entity].Command Valid(this Create[Entity].Command command)
        {
            // TODO: Return new valid command object.
            throw new NotImplementedException();
        }

        public static Create[Entity].Command Invalid(this Create[Entity].Command command)
        {
            // TODO: Return new valid command object.
            throw new NotImplementedException();
        }
    }
}
