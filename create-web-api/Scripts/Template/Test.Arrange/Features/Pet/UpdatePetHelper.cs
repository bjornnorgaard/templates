using System;
using Application.Features.[Entity];

namespace Test.Arrange.Features.[Entity]
{
    public static class Update[Entity]Helper
    {
        public static Update[Entity].Command Valid(this Update[Entity].Command command, Guid id)
        {
            // TODO: Return new valid command object.
            throw new NotImplementedException();
        }

        public static Update[Entity].Command Invalid(this Update[Entity].Command command, Guid id)
        {
            // TODO: Return new valid command object.
            throw new NotImplementedException();
        }
    }
}
